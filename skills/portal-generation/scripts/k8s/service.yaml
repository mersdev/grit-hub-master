apiVersion: v1
kind: Service
metadata:
  name: grit-hub-portal
  labels:
    app: grit-hub-portal
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: grit-hub-portal
