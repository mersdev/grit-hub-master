apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: grit-hub-portal
  labels:
    app: grit-hub-portal
spec:
  to:
    kind: Service
    name: grit-hub-portal
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
